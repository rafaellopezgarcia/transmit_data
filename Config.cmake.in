@PACKAGE_INIT@

if(CMAKE_COMPILER_IS_GNUCXX MATCHES 1)
  if(EXISTS "${CMAKE_CURRENT_LIST_DIR}/comTargets.cmake")
    message("com with gcc")
    include("${CMAKE_CURRENT_LIST_DIR}/comTargets.cmake")
  elseif(EXISTS "${CMAKE_CURRENT_LIST_DIR}/comTargetsClang.cmake")
    message("com with clang")
    include("${CMAKE_CURRENT_LIST_DIR}/comTargetsClang.cmake")  
  endif()
else()
  if(EXISTS "${CMAKE_CURRENT_LIST_DIR}/comTargetsClang.cmake")
    message("com with clang")
    include("${CMAKE_CURRENT_LIST_DIR}/comTargetsClang.cmake")
  elseif(EXISTS "${CMAKE_CURRENT_LIST_DIR}/comTargets.cmake")
    message("com with gcc")
    include("${CMAKE_CURRENT_LIST_DIR}/comTargets.cmake")  
  endif()
endif()
check_required_components(com)
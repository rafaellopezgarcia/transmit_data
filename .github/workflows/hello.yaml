name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Hello World!"
      - name: "Checkout the source code"
        uses: actions/checkout@v2
      - name: build project
        run: ls
        shell: bash
      - name: build project
        run: ./scripts/build
        shell: bash
